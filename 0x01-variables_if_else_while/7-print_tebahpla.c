#include <stdio.h>
/**
 * main - Represents Entry Point to the program
 *
 * Return: 0 to stop the program
 */
int main(void)
{
char a;

for (a = 'z'; a >= 'a' ; a--)
putchar(a);
putchar('\n');
return (0);
}
